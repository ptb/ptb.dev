/* eslint-disable import/order */

import { default as vikeReactConfig } from "vike-react/config"
import type { Config } from "vike/types"

export const config: Config = {
  extends: [vikeReactConfig]
}
