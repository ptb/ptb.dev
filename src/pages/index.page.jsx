import classNames from "./index.css.js"

/**
  @typedef {typeof import ("react")} React
 */

/**
  `Index` component.

  @returns {React.ReactElement}
    React component.
 */

export default function Index () {
  return <div className={classNames.root}>Hi</div>
}
