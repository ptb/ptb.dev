{
  "$schema": "https://json.schemastore.org/package.json",
  "devDependencies": {
    "@ptb/eslint-config": "2024.7.1",
    "@ptb/prettier-config": "2024.7.1",
    "@ptb/typescript-config": "2024.7.1",
    "lefthook": "1.6.18",
    "typescript": "5.5.2"
  },
  "engines": {
    "node": "^20.9 || ^22",
    "pnpm": "^10"
  },
  "private": true,
  "scripts": {
    "build:web": "pnpm --dir web build",
    "check:lint": "eslint --max-warnings 0 --no-warn-ignored --report-unused-disable-directives",
    "check:tidy": "prettier --check",
    "check:type": "pnpm --parallel --reporter-hide-prefix --stream -r '/check:type/'",
    "postinstall": "lefthook install"
  },
  "type": "module",
  "workspaces": [
    "./api",
    "./app",
    "./doc",
    "./lib",
    "./pkg/*",
    "./sql",
    "./web"
  ]
}
